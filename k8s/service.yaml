apiVersion: v1
kind: Service
metadata:
  labels:
    app: squad2-app
    app.kubernetes.io/instance: squad2-app
    app.kubernetes.io/name: squad2-app
  name: squad2-app-svc
spec:
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: 3000
  selector:
    app.kubernetes.io/instance: squad2-app
    app.kubernetes.io/name: squad2-app
  sessionAffinity: None
  type: ClusterIP
